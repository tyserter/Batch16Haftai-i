public class VariableAssigment {
    public void VariableAssigmentMethod() {
        string batchName='Batch 16';
        integer noOfStudents = 50;
        string description = 'This is the batch and we are doing fine ';

        system.debug(batchName);
        system.debug(noOfStudents);
        system.debug(description);

    }
}